import React from 'react';
import styles from '../../styles/chatbot/Avatar.module.css';

const Avatar: React.FC = () => {
	return (
		<div className={styles['ADE']}>ADE</div>
	);
}

export default Avatar;
